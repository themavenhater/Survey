{% extends 'bebe.html.twig' %}

{% block body %}
        <div class="container">
                <h1>{{ survey.title }}</h1>
                <p>{{ survey.description }}</p>

                {{ form(form) }}

        </div>
{% endblock %}

